# db

Data layer — models, repositories, migrations.

**Depends on:** nothing. This is the base layer.
**Must not depend on:** `api`, `app`, `infra`.

**Canonical example:** _pending._ The first repository added here becomes the
pattern the others replicate.
