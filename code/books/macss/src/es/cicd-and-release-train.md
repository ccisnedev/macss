# CI/CD y release train

The modular_api ecosystem runs as a multi-SDK package train:

- Dart
- TypeScript
- Python

Architecture documentation must stay synchronized with release operations:

- publish workflows,
- dependency-safe order,
- validation matrix and release checklist,
- post-merge verification evidence.

Documentation is considered complete only when architecture intent and release
mechanics remain aligned.
