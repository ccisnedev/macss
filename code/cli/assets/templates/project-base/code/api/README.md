# api

Application layer — use cases, services, orchestration.

**Depends on:** `db`.
**Must not depend on:** `app`, `infra`.

**Canonical example:** _pending._ The first use case added here becomes the
pattern the others replicate.
