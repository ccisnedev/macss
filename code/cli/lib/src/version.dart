/// Single source of truth for MACSS CLI version.
///
/// Update this constant when bumping version in pubspec.yaml.
/// Both are kept in sync manually — validated by version_sync_test.dart.
library;

const String macssVersion = '0.9.0';
